// Test301

program test301

	const int nula = 0;
	const int jedan = 1;
	const int tri = 3;
        const int pet = 5;

	int niz[];
	char nizch[];
	
{
	void main()	
		int bodovi;
		bool bt;
	{
		bodovi= 2 + ;  //GRESKA kd iskaza dodele
		bodovi = 0;
		bodovi ++;
		bodovi = (bodovi + jedan) ?? tri;
		//bodovi = (bodovi + ) ?? tri;
		bodovi = bodovi * pet;
		bodovi--;
		print(bodovi); 
								
		niz = new int[3];
		niz[nula] = jedan;
		niz[1] = 2;
		niz[niz[jedan] ?? -1] = niz[(niz[0] - jedan) ?? (bodovi * nula ?? jedan) ] * 3;
		bodovi = niz[2] / niz[0];
		print(bodovi); 
		print(niz[2]);
		
		
		nizch = new char[3];
		nizch[0] = 'a';
		nizch[jedan] = 'b';
		nizch[pet - 3] = 'c';
		print(nizch[1]);
		print(nizch[jedan * 2]);
			
		
		bodovi = bodovi + (pet * jedan - 1) * bodovi - (3 % 2 + 3 * 2 - 3); 
		print(bodovi);
		
		read(bodovi);
		print(bodovi);		
	}
	
	int k()
	bool i;
	int a;
	{
		
		i=true;
		a= 4;
		print(a,8);
		return 1;
	
	}
	
	void at(int a)char b;{
		read(b);
		print(a);
	}
}

